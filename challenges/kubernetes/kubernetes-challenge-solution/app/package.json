{
  "name": "desafio-api",
  "version": "1.0.0",
  "private": true,
  "description": "API do desafio Kubernetes (MySQL)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
