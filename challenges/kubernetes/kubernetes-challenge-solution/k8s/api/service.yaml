apiVersion: v1
kind: Service
metadata:
  name: desafio-api
  namespace: desafio-api
  labels:
    app: desafio-api
spec:
  type: ClusterIP
  selector:
    app: desafio-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
